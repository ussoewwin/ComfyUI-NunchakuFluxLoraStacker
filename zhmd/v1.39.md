<table align="center">
  <tr>
    <td align="center" bgcolor="#e5e7eb" width="88" height="36"><a href="https://github.com/ussoewwin/ComfyUI-NunchakuFluxLoraStacker/releases/tag/v1.39"><font color="#4b5563"><b>EN</b></font></a></td>
    <td align="center" bgcolor="#d4465e" width="88" height="36"><font color="#ffffff"><b>中文</b></font></td>
  </tr>
</table>

# v1.39 — AMD / ROCm nunchaku 导入防护与 FLUX 注册修复

本发行说明说明如何在官方 **`nunchaku`** 包无法导入时（常见于 **AMD / ROCm** 及其他非 CUDA 环境）仍能加载 **ComfyUI-NunchakuFluxLoraStacker**，以及如何避免在没有可用 nunchaku 时注册损坏的 FLUX 节点。

**相关**

- [PR #6](https://github.com/ussoewwin/ComfyUI-NunchakuFluxLoraStacker/pull/6) — AMD 兼容：对 nunchaku 导入加防护（贡献者：[0xDELUXA](https://github.com/0xDELUXA)，提交 `6608421`）
- 本仓库后续修复 — 提交 `2042e0c`（FLUX 注册门控、`standard` / `standard_v3` 导入范围、禁止 `compose_lora = None`）
- 技术说明：[`zhmd/ROCM_NUNCHAKU_IMPORT_REGISTRATION_FIX_EXPLANATION.md`](ROCM_NUNCHAKU_IMPORT_REGISTRATION_FIX_EXPLANATION.md)（英文：[md 同名文件](../md/ROCM_NUNCHAKU_IMPORT_REGISTRATION_FIX_EXPLANATION.md)）

---

## 1. 问题

在没有 nunchaku（或 nunchaku 导入失败）时，若对依赖 nunchaku 的模块做**未防护**导入，会在 ComfyUI 启动时导致**整个**自定义节点包加载失败。即使用户只需要本包中的非 FLUX 节点（Resolution Selector、Florence-2、Color Filter、CCSR、标准 LoRA 堆叠器等），也会全部丢失。

PR #6 正确地为 nunchaku 导入加入了 try/except，使整包仍可加载。但仅此还不够，仍有缺口：

1. 若 `wrappers/flux.py` 吞掉导入失败，但 FLUX 节点模块仍能成功导入，`__init__.py` 可能把「模块导入成功」当成「应注册 FLUX」。
2. 损坏的 FLUX 节点会出现在界面中，却没有可用的 nunchaku 后端。
3. 将 `compose_lora = None` 会把缺失依赖变成晚到的 `TypeError`（`NoneType` is not callable），而不是清晰失败。
4. 把非 nunchaku 加载器（`standard` / `standard_v3`）包进同一宽 try/except，会掩盖无关错误，并把失败误标为 `[ROCm]`。

---

## 2. v1.39 落地内容

### 2.1 合并 PR #6 — 导入防护

- `wrappers/flux.py`：对 `nunchaku` 导入 try/except；设置 `_NUNCHAKU_AVAILABLE`。
- `nodes/lora/flux.py` / `flux_v2.py`：对 `compose_lora` 导入加防护。
- `nodes/lora/sdnq.py`：保留 `traceback.print_exc()` 以便诊断。
- `__init__.py`：对 FLUX 相关导入 try/except，避免 nunchaku 失败拖垮整包加载。

### 2.2 后续 `2042e0c` — 注册与失败形态

| 关注点 | v1.39 之后的行为 |
|--------|------------------|
| 整包加载 | nunchaku 导入失败**不得**中止非 FLUX 节点注册。 |
| FLUX 注册 | **仅当** `_NUNCHAKU_AVAILABLE` 为真时注册 FLUX 节点。 |
| `compose_lora` | 绝不留下可调用的 `None`。缺少 nunchaku 时抛出明确的 **`RuntimeError`**。 |
| `standard` / `standard_v3` | 正常导入；**不**包在 nunchaku/ROCm try/except 中。 |
| 日志 | `[ROCm]` 类消息仅用于 nunchaku / FLUX 缺失。 |

**后续触及的文件：** `__init__.py`、`wrappers/flux.py`、`nodes/lora/flux.py`、`nodes/lora/flux_v2.py`。

---

## 3. 预期结果

1. **无 nunchaku / ROCm 导入失败：** ComfyUI 可加载本包。非 FLUX 节点会注册。FLUX 节点**不会**注册。
2. **NVIDIA + 可用 nunchaku：** FLUX 节点照常注册，行为与后续修复前一致。
3. **在无 nunchaku 时强行调用 compose：** 明确的 `RuntimeError`，而不是调用 `None` 的 `TypeError`。
4. **`standard` / `standard_v3` 失败：** 不按 ROCm/nunchaku 失败处理。

---

## 4. 如何应用

1. 将本自定义节点更新到 **v1.39**（git pull / ComfyUI-Manager Update / 本发行标签）。
2. **完全重启 ComfyUI**，使 `__init__.py` 与 wrappers 重新加载。
3. 确认节点列表：仅在 nunchaku 导入成功时出现 FLUX 加载器。
4. 在无 nunchaku 的 AMD / ROCm 上，确认本包其余节点仍出现。

---

## 5. 文档 / 更新日志

- 更新日志：[`md/CHANGELOG.md`](../md/CHANGELOG.md) / [`zhmd/CHANGELOG.md`](CHANGELOG.md) — v1.39 摘要
- 技术说明：[`zhmd/ROCM_NUNCHAKU_IMPORT_REGISTRATION_FIX_EXPLANATION.md`](ROCM_NUNCHAKU_IMPORT_REGISTRATION_FIX_EXPLANATION.md)

---

## 6. 范围之外

- 修改 ComfyUI 核心或其他自定义节点仓库。
- 要求 PR 作者实现注册门控后续（该工作已在本仓库完成）。
- Comfy Registry / `pyproject.toml` 版号 bump（除非另行 bump，否则不属于本发行说明表面）。

---

## 提交（标签 `v1.39`）

- `6608421` — AMD compatibility: guard nunchaku imports（PR #6）
- `fa552cd` — Merge pull request #6 from 0xDELUXA/amd-compat
- `2042e0c` — fix: gate FLUX registration on nunchaku availability after AMD compat merge
- `1587042` — docs: ROCm nunchaku import and FLUX registration fix explanation
- `5f0f0ca` — docs: CHANGELOG entry for v1.39
